export enum ExplorerFrameCommand {
	Normal = 0x00,
	InclusionRequest = 0x01,
	SearchResult = 0x02,
}

export enum ChannelConfiguration {
	"Classic" = 0,
	"Classic & LR A" = 1,
	"Classic & LR B" = 2,
	"LR A & B" = 3,
}
